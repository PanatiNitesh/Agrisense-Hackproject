[build]
  base = "agrisense" 
  command = "npm install && npm run build"            
  publish = "dist" 

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
[secrets_scan]
  omit_keys = ["VITE_BACK_URI"]
